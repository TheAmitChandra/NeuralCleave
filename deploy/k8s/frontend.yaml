apiVersion: apps/v1
kind: Deployment
metadata:
  name: NeuralCleave-frontend
  namespace: NeuralCleave
  labels:
    app: NeuralCleave-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: NeuralCleave-frontend
  template:
    metadata:
      labels:
        app: NeuralCleave-frontend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: frontend
          image: NeuralCleave/frontend:latest
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_URL
              value: "http://neuralcleave-backend:8000"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: NeuralCleave-frontend
  namespace: NeuralCleave
spec:
  selector:
    app: NeuralCleave-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
